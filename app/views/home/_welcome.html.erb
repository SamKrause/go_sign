<div id="welcome">
  <h1 class="profiletitle">Welcome <%= current_user.first_name %></h1>
  <div class="username"><%= image_tag(current_user.avatar.url(:thumb)) %></div><%= link_to 'Tests', tests_index_path, :method => :get %> 
  <div class="profileLogOut">
  <%= link_to "Log Out", destroy_user_session_path, :id => "logoutbutton", :method => :delete if current_user %></div>
</div>
