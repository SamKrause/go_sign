<div id="welcome">
  <h1>Welcome <%= current_user.first_name %></h1>
  <div class="username"><%= image_tag(current_user.avatar.url(:thumb)) %></div>
  <%= link_to "Log Out", destroy_user_session_path, :id => "logoutbutton", :method => :delete if current_user %>
</div>
